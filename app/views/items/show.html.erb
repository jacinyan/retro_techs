<p id="notice"><%= notice %></p>

<%# <div class="container">
  <div class="card-deck">  %>

    <%# <div class="card mb-4" style="min-width: 18rem;"> %>
    <%= image_tag @item.picture, class: 'card-img-top' if @item.picture.attached? %>
    
      <tr>
        <th>Name:</th>
        <td><%= @item.name %></td>
      </tr>

      <tr>
        <th>In stock:</th>
        <td><%= @item.in_stock %></td>
      </tr>

      <tr>
        <th>Price:</th>
        <td><%= @item.price %></td>
      </tr>

      <tr>
        <th>Description:</th>
        <td><%= @item.description %></td>
      </tr>
      
  

      <%# </div> %>

  <%# </div> %>

  <%# </div>
</div> %>

  <%=link_to 'Edit', edit_item_path(@item), class: "btn btn-warning" if can? :update, @item%>

<%=link_to 'Delete', item_path(@item), class: "btn btn-danger", method: "delete", data: { confirm: "Are you sure?" } if can? :destroy, @item %> ||


<%# <%=link_to 'Add to Order', orders_path, method: :post, class: "btn btn-warning" if can? :manage, Order%> 

 <%= button_to "Add to cart", order_items_path( item_id: @item.id), method: :post, class: "btn btn-warning" if can? :manage, Order %> 

<%= link_to 'Back', items_path %>


