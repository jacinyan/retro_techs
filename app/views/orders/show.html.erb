<%=link_to 'Edit', edit_order_path(@order), class: "btn btn-warning" if can? :update, @order%>

<%=link_to 'Delete', order_path(@order), class: "btn btn-danger", method: "delete", data: { confirm: "Are you sure?" } if can? :destroy, @order %> ||

<%= link_to 'Back', orders_path %>